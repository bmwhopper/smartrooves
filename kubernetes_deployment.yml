apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hackathon
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: smartrooves
    spec:
      containers:
      - name: hackathon
        image: registry/repository
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          protocol: TCP
      imagePullSecrets:
      - name: ocirsecret 
      restartPolicy: Always
---
apiVersion: latest
kind: Service
metadata:
  name: smartrooves
  labels:
    app: smartrooves
spec:
  ports:
  - port: 8080
    name: http
  selector:
    app: smartrooves
