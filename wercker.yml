box: 
  id: node:latest
  ports: 
    - "8080"
    
build:
  steps:
    - npm-install
    - internal/watch:
        code: node index.js
        reload: true
test:
  steps:
    - script:
        - npm-test

